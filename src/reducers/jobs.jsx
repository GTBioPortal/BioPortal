// return default state of job based on action type
const jobs = (state = [], action) => {
    switch(action.type) {
        default:
            return state;
    }
}

// export const for use
export default jobs;
